import 'package:flutter/material.dart';

class GstFiling extends StatelessWidget {
  const GstFiling({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(// Custom Header
      body: Center(
        child: Text(
          'GST Filing', // Replace this with actual content
          style: TextStyle(fontSize: 24), // Example styling
        ),
      ),
    );
  }
}
